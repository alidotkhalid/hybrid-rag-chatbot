# ---- Web service -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

# ---- Retrieval -------------------------------------------------------------
# CPU-only torch keeps the image ~2GB instead of ~7GB. The extra index URL is
# declared in the Dockerfile rather than here so that `pip install -r` still
# works unmodified on a machine with CUDA.
torch==2.5.1
sentence-transformers==3.3.1
faiss-cpu==1.9.0.post1
numpy==1.26.4
scipy==1.14.1

# ---- Document parsing ------------------------------------------------------
pymupdf==1.25.1
python-docx==1.1.2
markdown-it-py==3.0.0

# ---- LLM providers (all optional at runtime, selected by RAG_LLM_PROVIDER) --
httpx==0.28.1

# ---- Utilities -------------------------------------------------------------
tenacity==9.0.0
orjson==3.10.13
